{
  "name": "divgrow-monorepo",
  "private": true,
  "scripts": {
    "dev": "cd stock && npm run dev",
    "build": "cd stock && npm run build",
    "vercel-build": "cd stock && npm install && npm run build"
  },
  "dependencies": {},
  "devDependencies": {}
}
