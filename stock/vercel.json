{
    "headers": [
        {
            "source": "/api/(.*)",
            "headers": [{ "key": "Access-Control-Allow-Origin", "value": "*" }]
        }
    ],
    "rewrites": [
        {
            "source": "/:path((?!api/|.*\\.).*)",
            "destination": "/index.html"
        }
    ]
}
